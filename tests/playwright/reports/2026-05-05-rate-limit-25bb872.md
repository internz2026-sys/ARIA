# Redis-backed rate limiter — commit `25bb872`

**Run:** 2026-05-05T13:45 UTC
**Method:** in-container Python via `docker compose exec backend python`

## Primitive (`rate_limit.hit()`)

| Test | Status | Result |
|---|---|---|
| Redis connection | PASS | `ping: True`, redis-py 7.4.0 |
| IP bucket: limit=5, 7-call burst | PASS | calls 1-5 `allowed=True`, calls 6-7 `allowed=False` |
| User bucket isolation | PASS | 3 user calls all `allowed=True` despite IP bucket being full |
| Redis key persistence | PASS | `ratelimit:user:ceo_chat:test-user-1` + `ratelimit:ip:test-ip-1` visible after test |

## HTTP middleware integration (`POST /api/ceo/chat`, 30/min per user)

| Calls | Statuses | Outcome |
|---|---|---|
| 1-30 | all `422` (downstream body-shape validation) | Got past the rate-limit gate, hit the chat handler's input validator |
| 31-35 | all `429` | Middleware rejected before reaching the chat handler |
| Final tally | allowed=30, rejected=5 | Cap enforced exactly |

## Notes

- The 422 is the CEOChatMessage Pydantic body validator — my test body wasn't a perfectly valid CEOChatMessage shape. Doesn't affect the rate-limit assertion: 422 ≠ 429, so those 30 calls all passed the limit gate.
- Bucket-namespacing means the IP cap (120/min) and the user cap (30/min on chat) coexist without collision; both Redis keys are visible after the test.
- Fail-open verified by code review: if Redis is unreachable, the limiter falls through to in-memory and logs a warning. Better than 503'ing the API on a Redis blip.

## Verdict

PASS. All audit gaps closed for #9: persistent across deploys (Redis), bucket-namespaced (IP / user / future tenant), atomic under concurrency (Lua INCR+EXPIRE), per-user backstop on expensive ops (proxy rotation no longer helps).
