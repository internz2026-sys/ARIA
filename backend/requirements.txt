fastapi>=0.115.0
uvicorn[standard]>=0.30.0
supabase>=2.9.0
python-dotenv>=1.0.1
python-multipart>=0.0.9
openpyxl>=3.1.5
httpx>=0.27.0
pydantic>=2.10.0
python-socketio>=5.11.0
stripe>=10.0.0
sendgrid>=6.11.0
twilio>=9.3.0
sentry-sdk[fastapi]>=2.14.0
pinecone-client>=5.0.0
tenacity>=9.0.0
qdrant-client>=1.9.0
sentence-transformers>=3.0.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# matplotlib renders branded chart PNGs for Ad Strategist [GRAPH_DATA]
# blocks. Agg backend (server.py forces it before any pyplot import) so
# Docker doesn't need X11 / a display.
matplotlib>=3.8.0
# Paperclip AI orchestration is a local Node.js server (localhost:3100)
# integrated via REST API using httpx — no Python package needed
# Redis client — backs the per-tenant / per-user rate limiter and any
# future shared cache. The aria-redis container is already in the
# stack; this just gives the backend a Python client for it.
redis>=5.0.0
